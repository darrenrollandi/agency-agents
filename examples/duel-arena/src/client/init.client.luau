--!strict
-- Client entry point. Bootstraps Controllers (P1+):
-- CameraController → WeaponController → AbilityController → HudController
-- → LobbyController → LockerController.
print("[Client] Duel Arena client booted")
